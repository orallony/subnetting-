<div dir="rtl" align="right">

# Subnetting למתחילים

## מהי רשת מחשבים?

רשת מחשבים היא אוסף של התקנים שמחוברים אחד לשני ויכולים להעביר מידע ביניהם.

לדוגמה:

- מחשב
- שרת
- מדפסת
- מצלמת אבטחה
- טלפון

כל ההתקנים הללו יכולים להיות מחוברים לאותו `Switch` ולתקשר ביניהם.

---

## למה בכלל צריך כתובות?

נניח שיש בכיתה 30 תלמידים.

אם המורה צועק:

> "היי!"

כולם שומעים.

אבל אם המורה אומר:

> "אור, בוא רגע."

רק אור יודע שפנו אליו.

בדיוק כך עובד מחשב ברשת.

כאשר מחשב רוצה לדבר עם מחשב אחר, הוא חייב לדעת למי לפנות.

לכן לכל התקן ברשת יש כתובת.

---

## מהי כתובת `IP`?

כתובת `IP` היא כתובת ייחודית שמזהה התקן ברשת.

לדוגמה:

</div>

```text
192.168.1.10
```

<div dir="rtl" align="right">

המטרה של כתובת `IP` היא לענות על שתי שאלות:

1. לאיזו רשת שייך ההתקן?
2. איזה התקן הוא בתוך אותה רשת?

---

## מבנה כתובת `IPv4`

כתובת `IPv4` מורכבת מארבעה מספרים.

לדוגמה:

</div>

```text
192.168.1.10
```

<div dir="rtl" align="right">

| אוקטטה 1 | אוקטטה 2 | אוקטטה 3 | אוקטטה 4 |
|---|---|---|---|
| 192 | 168 | 1 | 10 |

כל חלק נקרא `Octet`.

---

## חוקי כתובת `IPv4`

**חוק מספר 1 – חייבות להיות 4 אוקטטות**

תקין:

</div>

```text
192.168.1.10
```

<div dir="rtl" align="right">

לא תקין:

</div>

```text
192.168.1
192.168.1.10.20
```

<div dir="rtl" align="right">

**חוק מספר 2 – כל אוקטטה חייבת להיות בין 0 ל־255**

תקין:

</div>

```text
192.168.1.10
255.255.255.255
```

<div dir="rtl" align="right">

לא תקין:

</div>

```text
300.168.1.10
192.168.500.1
```

<div dir="rtl" align="right">

**חוק מספר 3 – אסור להשתמש באותיות**

תקין:

</div>

```text
192.168.1.10
```

<div dir="rtl" align="right">

לא תקין:

</div>

```text
192.168.A.10
```

<div dir="rtl" align="right">

**חוק מספר 4 – לכל מחשב חייבת להיות כתובת ייחודית**

לא יכולים להיות שני מחשבים באותה רשת עם אותה כתובת `IP`.

**חוק מספר 5 – יש כתובות פרטיות**

הטווחים הפרטיים הם:

</div>

```text
10.0.0.0 - 10.255.255.255
172.16.0.0 - 172.31.255.255
192.168.0.0 - 192.168.255.255
```

<div dir="rtl" align="right">

**חוק מספר 6 – כתובת `Loopback`**

</div>

```text
127.0.0.1
```

<div dir="rtl" align="right">

זו כתובת מיוחדת שמאפשרת למחשב לפנות לעצמו.

---

## למה דווקא 255?

כדי להבין זאת צריך להבין איך מחשב שומר מידע.

---

## מהו `Bit`?

`Bit` הוא יחידת המידע הקטנה ביותר במחשב.

הוא יכול להכיל רק:

</div>

```text
0
1
```

<div dir="rtl" align="right">

---

## למה מחשבים עובדים עם 0 ו־1?

המחשב יודע לזהות שני מצבים בסיסיים:

</div>

```text
יש מתח
אין מתח
```

<div dir="rtl" align="right">

לכן הוא משתמש בשיטה שנקראת `Binary`.

---

## מהו `Byte`?

`Byte` הוא קבוצה של 8 ביטים.

לדוגמה:

</div>

```text
10101010
```

<div dir="rtl" align="right">

---

## הסרגל הבינארי

לכל ביט יש ערך.

</div>

```text
128 64 32 16 8 4 2 1
```

<div dir="rtl" align="right">

כל מספר הוא כפול מהמספר שאחריו.

---

## איך מחשבים מספר בבינארי?

נניח שיש לנו:

</div>

```text
128 64 32 16 8 4 2 1
 0   1  0  0 1 1 1 0
```

<div dir="rtl" align="right">

מחברים רק את הערכים שמעליהם יש 1:

</div>

```text
64 + 8 + 4 + 2 = 78
```

<div dir="rtl" align="right">

לכן:

</div>

```text
01001110 = 78
```

<div dir="rtl" align="right">

---

## למה 255 הוא המספר המקסימלי?

אם כל הביטים דלוקים:

</div>

```text
11111111
```

<div dir="rtl" align="right">

מקבלים:

</div>

```text
128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 = 255
```

<div dir="rtl" align="right">

לכן:

</div>

```text
00000000 = 0
11111111 = 255
```

<div dir="rtl" align="right">

ומכאן מגיע הכלל:

> כל אוקטטה יכולה להכיל מספר בין 0 ל־255.

---

## כמה ביטים יש בכתובת `IP`?

יש 4 אוקטטות.

בכל אוקטטה יש 8 ביטים.

לכן:

</div>

```text
8 + 8 + 8 + 8 = 32
```

<div dir="rtl" align="right">

כתובת `IPv4` מורכבת מ־32 ביטים.

---

## כתובת `IP` מכילה שני סוגי מידע

כל כתובת `IP` חייבת לענות על שתי שאלות:

1. לאיזו רשת שייך המחשב?
2. איזה מחשב הוא בתוך הרשת?

לכן כל כתובת מחולקת לשני חלקים:

</div>

```text
Network
Host
```

<div dir="rtl" align="right">

---

## מהו `CIDR`?

`CIDR` הוא המספר שמופיע אחרי הסימן `/`.

לדוגמה:

</div>

```text
192.168.1.10/24
```

<div dir="rtl" align="right">

המספר `24` אומר:

> 24 ביטים שייכים לרשת.

---

## מה נשאר למחשבים?

לכתובת `IPv4` יש 32 ביטים.

אם 24 ביטים שייכים לרשת:

</div>

```text
32 - 24 = 8
```

<div dir="rtl" align="right">

נשארו 8 ביטים למחשבים.

---

## איך נראה `/24` בבינארי?

</div>

```text
11111111.11111111.11111111.00000000
```

<div dir="rtl" align="right">

נספור את הביטים הדלוקים:

</div>

```text
8 + 8 + 8 = 24
```

<div dir="rtl" align="right">

לכן זה נקרא `/24`.

---

## מהו `Subnet Mask`?

`Subnet Mask` הוא דרך נוספת לכתוב את אותו מידע של `CIDR`.

לדוגמה:

</div>

```text
/24 = 255.255.255.0
```

<div dir="rtl" align="right">

למה?

כי:

</div>

```text
11111111 = 255
11111111 = 255
11111111 = 255
00000000 = 0
```

<div dir="rtl" align="right">

לכן:

</div>

```text
11111111.11111111.11111111.00000000
=
255.255.255.0
```

<div dir="rtl" align="right">

---

## איך מזהים את חלק הרשת?

אם יש כתובת:

</div>

```text
192.168.1.10/24
```

<div dir="rtl" align="right">

ה־`/24` אומר ש־24 הביטים הראשונים שייכים לרשת.

כלומר:

</div>

```text
192.168.1 = Network
10          = Host
```

<div dir="rtl" align="right">

---

## מהי כתובת `Network ID`?

`Network ID` היא הכתובת הראשונה ברשת.

לדוגמה, ברשת:

</div>

```text
192.168.1.0/24
```

<div dir="rtl" align="right">

כתובת ה־`Network ID` היא:

</div>

```text
192.168.1.0
```

<div dir="rtl" align="right">

לא ניתן להקצות אותה למחשב.

---

## מהי כתובת `Broadcast`?

`Broadcast` היא הכתובת האחרונה ברשת.

לדוגמה, ברשת:

</div>

```text
192.168.1.0/24
```

<div dir="rtl" align="right">

כתובת ה־`Broadcast` היא:

</div>

```text
192.168.1.255
```

<div dir="rtl" align="right">

גם אותה לא ניתן להקצות למחשב.

---

## כמה כתובות יש ב־`/24`?

ב־`/24` נשארים 8 ביטים למחשבים.

לכן:

</div>

```text
2^8 = 256
```

<div dir="rtl" align="right">

יש 256 כתובות בסך הכול.

אבל מורידים שתי כתובות:

- `Network ID`
- `Broadcast`

לכן:

</div>

```text
256 - 2 = 254
```

<div dir="rtl" align="right">

יש 254 כתובות שימושיות.

---

## למה צריך `Subnetting`?

נניח שיש בארגון כמה מחלקות:

- הנהלה
- כספים
- `IT`
- אורחים

אם כולם יהיו באותה רשת, יהיה קשה לנהל ולאבטח אותה.

לכן מחלקים את הרשת הגדולה לכמה רשתות קטנות יותר.

---

## מהו `Subnet`?

`Subnet` הוא קיצור של:

</div>

```text
Sub Network
```

<div dir="rtl" align="right">

כלומר:

</div>

```text
תת־רשת
```

<div dir="rtl" align="right">

---

## מהו `Subnetting`?

`Subnetting` הוא תהליך של חלוקת רשת גדולה לכמה רשתות קטנות יותר.

---

## תרגיל

נתונה הרשת:

</div>

```text
192.168.10.0/24
```

<div dir="rtl" align="right">

חלק אותה ל־4 תתי־רשתות.

מצא:

1. מהו ה־`CIDR` החדש?
2. מהו ה־`Subnet Mask` החדש?
3. כמה כתובות יש בכל `Subnet`?
4. כמה כתובות שימושיות יש בכל `Subnet`?
5. מהם הטווחים של כל `Subnet`?

---

## פתרון

**שלב 1 – כמה רשתות צריך?**

צריך 4 רשתות.

מחפשים חזקה של 2 שנותנת 4:

</div>

```text
2^1 = 2
2^2 = 4
```

<div dir="rtl" align="right">

לכן צריך לקחת 2 ביטים.

---

**שלב 2 – מחשבים `CIDR` חדש**

התחלנו ב־`/24`.

מוסיפים 2 ביטים:

</div>

```text
24 + 2 = 26
```

<div dir="rtl" align="right">

לכן ה־`CIDR` החדש הוא:

</div>

```text
/26
```

<div dir="rtl" align="right">

---

**שלב 3 – מחשבים `Subnet Mask`**

`/26` בבינארי נראה כך:

</div>

```text
11111111.11111111.11111111.11000000
```

<div dir="rtl" align="right">

האוקטטה האחרונה היא:

</div>

```text
11000000
```

<div dir="rtl" align="right">

לפי הסרגל הבינארי:

</div>

```text
128 64 32 16 8 4 2 1
 1   1  0  0 0 0 0 0
```

<div dir="rtl" align="right">

מחברים:

</div>

```text
128 + 64 = 192
```

<div dir="rtl" align="right">

לכן:

</div>

```text
/26 = 255.255.255.192
```

<div dir="rtl" align="right">

---

**שלב 4 – מחשבים קפיצה**

הנוסחה:

</div>

```text
256 - הערך באוקטטה שנשברה
```

<div dir="rtl" align="right">

במקרה שלנו:

</div>

```text
256 - 192 = 64
```

<div dir="rtl" align="right">

הקפיצה היא 64.

---

**שלב 5 – מוצאים את הרשתות**

סופרים בקפיצות של 64:

</div>

```text
0
64
128
192
```

<div dir="rtl" align="right">

כל מספר כזה הוא התחלה של `Subnet` חדש.

---

## פריסת הרשתות

</div>

```text
Subnet 1
Network:   192.168.10.0
Usable:    192.168.10.1 - 192.168.10.62
Broadcast: 192.168.10.63

Subnet 2
Network:   192.168.10.64
Usable:    192.168.10.65 - 192.168.10.126
Broadcast: 192.168.10.127

Subnet 3
Network:   192.168.10.128
Usable:    192.168.10.129 - 192.168.10.190
Broadcast: 192.168.10.191

Subnet 4
Network:   192.168.10.192
Usable:    192.168.10.193 - 192.168.10.254
Broadcast: 192.168.10.255
```

<div dir="rtl" align="right">

---

## טבלת עזר

| `CIDR` | `Subnet Mask` | כתובות שימושיות |
|---|---|---|
| `/24` | `255.255.255.0` | 254 |
| `/25` | `255.255.255.128` | 126 |
| `/26` | `255.255.255.192` | 62 |
| `/27` | `255.255.255.224` | 30 |
| `/28` | `255.255.255.240` | 14 |
| `/29` | `255.255.255.248` | 6 |
| `/30` | `255.255.255.252` | 2 |

---

## סיכום

כדי להבין `Subnetting`, צריך להבין את השרשרת הבאה:

</div>

```text
רשת
↓
כתובת IP
↓
Octet
↓
Bit
↓
Binary
↓
255
↓
CIDR
↓
Subnet Mask
↓
Network / Host
↓
Network ID
↓
Broadcast
↓
Subnetting
```

<div dir="rtl" align="right">

כאשר מבינים את השרשרת הזאת, `Subnetting` הופך לנושא פשוט והגיוני.

</div>

## שיטת עבודה לפתרון כל תרגיל Subnetting

כאשר מקבלים תרגיל, תמיד עובדים לפי אותם השלבים.

### שלב 1 – מזהים את ה־CIDR המקורי

לדוגמה:

```text
192.168.10.0/24
```

ה־CIDR המקורי הוא:

```text
/24
```

---

### שלב 2 – מזהים לכמה תתי־רשתות צריך לחלק

לדוגמה:

```text
חלק ל־16 תתי־רשתות
```

לכן:

```text
16 Subnets
```

---

### שלב 3 – מחשבים כמה ביטים צריך להוסיף

מחפשים חזקה של 2 שנותנת את מספר תתי־הרשתות.

דוגמה:

```text
2¹ = 2
2² = 4
2³ = 8
2⁴ = 16
```

לכן:

```text
צריך להוסיף 4 ביטים
```

---

### שלב 4 – מחשבים CIDR חדש

הנוסחה:

```text
CIDR מקורי + מספר הביטים שהוספנו
```

דוגמה:

```text
24 + 4 = 28
```

לכן:

```text
CIDR חדש = /28
```

---

### שלב 5 – מוצאים את ה־Subnet Mask

מחפשים בטבלת העזר.

דוגמה:

```text
/28 = 255.255.255.240
```

---

### שלב 6 – מחשבים כמה כתובות יש בכל Subnet

הנוסחה:

```text
2^(32 - CIDR חדש)
```

דוגמה:

```text
2^(32-28)
=
2^4
=
16
```

לכן:

```text
16 כתובות בכל Subnet
```

---

### שלב 7 – מחשבים כמה כתובות שימושיות יש

הנוסחה:

```text
מספר כתובות - 2
```

מורידים:

- Network ID
- Broadcast

דוגמה:

```text
16 - 2 = 14
```

לכן:

```text
14 כתובות שימושיות
```

---

### שלב 8 – מחשבים את גודל הקפיצה

הנוסחה:

```text
256 - הערך באוקטטה שנשברה
```

דוגמה:

```text
255.255.255.240
```

האוקטטה שנשברה:

```text
240
```

לכן:

```text
256 - 240 = 16
```

הקפיצה היא:

```text
16
```

---

### שלב 9 – רושמים את כל הרשתות

סופרים בקפיצות.

דוגמה:

```text
0
16
32
48
64
80
96
112
128
144
160
176
192
208
224
240
```

כל מספר כזה הוא תחילתה של רשת חדשה.

---

### שלב 10 – מוצאים Network ID ו־Broadcast

הכתובת הראשונה בכל טווח היא:

```text
Network ID
```

הכתובת האחרונה בכל טווח היא:

```text
Broadcast
```

לדוגמה:

```text
192.168.10.0/28
```

טווח:

```text
192.168.10.0 - 192.168.10.15
```

לכן:

```text
Network ID = 192.168.10.0
Broadcast = 192.168.10.15
```

כתובות שימושיות:

```text
192.168.10.1 - 192.168.10.14
```

---

## נוסחת הקסם

בכל תרגיל:

```text
1. כמה Subnets צריך?
2. כמה ביטים צריך להוסיף?
3. CIDR חדש
4. Subnet Mask חדש
5. מספר כתובות
6. מספר כתובות שימושיות
7. גודל קפיצה
8. פריסת הרשתות
```

אם עובדים לפי הסדר הזה, אפשר לפתור כל תרגיל Subnetting.
